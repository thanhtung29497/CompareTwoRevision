"use strict";
/**
 * The `configuration` module allows the Model SDK user to configure
 * how to communicate with the Model API Server;
 * in particular w.r.t.: authentication, success/error handling, etc.
 */
var configuration;
(function (configuration) {
    function assertBackendAccess(config) {
        if (!config || !config.credentials || !config.credentials.username || !config.credentials.password) {
            throw new Error("Mendix Model SDK config: credentials are not defined. Backend access credentials are necessary to perform this operation.");
        }
    }
    configuration.assertBackendAccess = assertBackendAccess;
    function checkWorkingCopyId(workingCopyId) {
        if (!workingCopyId) {
            throw new Error("Mendix Model SDK config: workingCopyId is not defined.");
        }
    }
    configuration.checkWorkingCopyId = checkWorkingCopyId;
    function checkCallbacks(callback, errorCallback) {
        if (!callback) {
            throw new Error("Mendix Model SDK config: success callback is not defined.");
        }
        if (!errorCallback) {
            throw new Error("Mendix Model SDK config: error callback is not defined.");
        }
    }
    configuration.checkCallbacks = checkCallbacks;
})(configuration = exports.configuration || (exports.configuration = {}));

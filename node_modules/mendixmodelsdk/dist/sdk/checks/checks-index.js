"use strict";
var checks_runner_1 = require("./checks-runner");
var queries_1 = require("./queries");
var name_util_1 = require("./name-util");
var checks;
(function (checks) {
    checks.runChecksOn = checks_runner_1.runChecksOn;
    checks.makeChecksResolver = checks_runner_1.makeChecksResolver;
    checks.makeQueryResolver = queries_1.makeQueryResolver;
    /**
     * Validates the name syntactically without looking at the context (so no uniqueness check).
     * @returns {string} with error message or null in which case name is valid.
     */
    checks.validateNameSyntax = name_util_1.validateSyntax;
})(checks = exports.checks || (exports.checks = {}));

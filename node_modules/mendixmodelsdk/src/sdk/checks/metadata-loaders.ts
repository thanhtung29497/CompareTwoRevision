import {ICheck} from "./checks-types";
import {IQueryMap, ISuperTypeMap, IQuery} from "./queries";
	// (Note: tslint warns that IQuery is unused but it's implicitly used and required by the compiler.)

export const loadChecks = () => require("../../../metadata/checks.json") as ICheck[];
export const loadQueries = () => require("../../../metadata/queries.json") as IQueryMap;
export const loadSuperTypes = () => require("../../../metadata/super-types.json") as ISuperTypeMap;

